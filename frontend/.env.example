# Copy this file to .env and set your PC's IP (same Wi-Fi as your phone).
# Get the IP from the Backend terminal when you run: npm run dev
# Then restart Expo (press r in the Expo terminal or restart npm run start).
EXPO_PUBLIC_API_URL=http://192.168.1.100:8080
